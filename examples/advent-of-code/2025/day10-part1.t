#!/usr/bin/env topal
use language (version is v0.1)
use library advent-of-code (version is v0.1)
minimum is advent-of-code machine minimum-indicator-presses
solve is fn (input : String) -> Int
  minimum input
